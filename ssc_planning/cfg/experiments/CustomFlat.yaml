# Experiment (City Building) specific parameters

# Bounding boxes (in initialization frame, relative to PlayerStart)
map_bounding_volume:
  x_min: -20 #6.8 #-5.3 #-6.8
  x_max: 20 #11.0 #9.0
  y_min: -20
  y_max: 20
  z_min: -10
  z_max: 10

target_bounding_volume:
  x_min: -20 #6.8 #-5.3 #-6.8
  x_max: 20 #11.0 #9.0
  y_min: -20
  y_max: 20
  z_min: -10
  z_max: 10

# System Constraints
planner:
  system_constraints:
    v_max: 3.0
    a_max: 3.0
    yaw_rate_max: 1.57
    collision_radius: 0.2
